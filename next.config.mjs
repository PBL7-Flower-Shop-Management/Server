/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "happyflower.vn",
            "th.bing.com",
            "kenh14cdn.com",
            "product.hstatic.net",
        ], // Add 'th.bing.com' to the list
    },
};

export default nextConfig;
